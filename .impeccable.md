## Design Context

### Users

该界面面向在本地开发、联调和排查 Agent Runtime 的开发者。核心任务是快速发送多轮消息，同时确认接口地址、会话标识、运行标识、轮次、耗时和原始响应。

### Brand Personality

克制、可靠、工具化。界面应像一台清晰的调试仪器，强调信息层级和运行反馈，不营造面向消费者的拟人化聊天氛围。

### Aesthetic Direction

采用深色工业控制台风格，使用暖橙色作为少量操作强调色。布局保持紧凑但不拥挤，桌面端同时展示对话和调试信息，移动端保留全部关键能力。

### Design Principles

1. 对话优先，调试数据始终可见但不抢占阅读焦点。
2. 每次请求都提供明确的待命、运行、成功、失败状态。
3. 使用真实接口元数据，不在前端伪造运行信息。
4. 零外部前端依赖，确保本地示例启动后即可使用。
5. 键盘操作、焦点状态和低动态偏好均得到支持。
